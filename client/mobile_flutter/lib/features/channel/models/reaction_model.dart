// =============================================================================
// 反应模型（重新导出）
// =============================================================================
//
// 本文件重新导出公共反应模型，保持频道模块的向后兼容性。
//
// ## 设计说明
//
// 反应（Reaction）是跨模块共享的功能，因此核心模型定义在 `pkg/models/` 中。
// 频道模块通过重新导出的方式使用，这样：
// 1. 频道模块内部可以直接从 `channel_models.dart` 导入
// 2. 如果未来需要频道特有的反应逻辑，可以在此文件扩展
//
// ## 包含的类型
//
// - `ReactionStats` - 反应统计数据（emoji -> 数量的映射）
// - `ReactionSummary` - 单个反应的 UI 展示数据

export '../../../pkg/models/reaction_model.dart';
