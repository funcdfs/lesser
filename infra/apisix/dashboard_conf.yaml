# APISIX Dashboard 配置
# 管理员: funcdfs / fw142857

conf:
  listen:
    host: 0.0.0.0
    port: 9000
  
  etcd:
    endpoints:
      - etcd:2379
    prefix: /apisix
  
  log:
    error_log:
      level: warn
      file_path: /dev/stderr
    access_log:
      file_path: /dev/stdout

authentication:
  secret: fw142857-jwt-secret-key
  expire_time: 3600
  users:
    - username: funcdfs
      password: fw142857
    - username: admin
      password: admin
