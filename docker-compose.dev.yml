services:
  postgres:
    image: postgres:18
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./docker/postgres/data:/var/lib/postgresql
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_INITDB_ARGS: "-A trust"
    command:
      - postgres
      - -c
      - listen_addresses=0.0.0.0

  redis:
    image: redis:8
    command:
      - redis-server
      - --appendonly
      - yes
    restart: unless-stopped
    volumes:
      - ./docker/redis/data:/data
    ports:
      - "6379:6379"

  # meilisearch:
  #   image: getmeili/meilisearch:v1.8
  #   environment:
  #     MEILI_NO_ANALYTICS: "true"
  #   volumes:
  #     - ./docker/meilisearch/data:/meili_data
  #   ports: ["7700:7700"]

# todo÷÷
  # django:
  #   build: ./backend/django_code
  #   command: python manage.py runserver 0.0.0.0:8000
  #   restart: unless-stopped
  #   volumes:
  #     - ./backend/django_code:/app
  #   env_file: .env
  #   ports: ["8000:8000"]
  #   depends_on:
  #     - postgres
  #     - redis
  #     # - meilisearch
  #   healthcheck:
  #     test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

  # golang-service:
  #   build: ./backend/golang-code
  #   ports: ["7701:7701"]
