# 项目架构梳理

> 本文档详细描述项目的整体架构、数据流程和各端实现细节。

---

## 目录

1. [整体架构概览](#整体架构概览)
2. [前端架构 (Flutter)](#前端架构-flutter)
3. [后端架构](#后端架构)
   - [Gateway + Worker 架构](#gateway--worker-架构)
   - [Chat 服务架构](#chat-服务架构)
4. [新增路由修改流程](#新增路由修改流程)
5. [配置参数汇总](#配置参数汇总)

---

## 整体架构概览

```mermaid
graph TB
    subgraph 客户端层["📱 客户端层"]
        Flutter["Flutter Mobile/Web"]
        React["React Web (规划中)"]
    end

    subgraph 网关层["🌐 网关层"]
        Traefik["Traefik Gateway<br/>统一入口 :80"]
    end

    subgraph 服务层["⚙️ 服务层"]
        Gateway["Go Gateway<br/>gRPC :50053<br/>(Auth 同步 + Command/Query 分发)"]
        Chat["Go Chat Service<br/>gRPC :50052 / WS :8081"]
        
        subgraph Workers["Worker 集群"]
            UserWorker["User Worker"]
            PostWorker["Post Worker"]
            FeedWorker["Feed Worker"]
            NotifWorker["Notification Worker"]
            SearchWorker["Search Worker"]
        end
    end

    subgraph 消息层["📨 消息层"]
        RabbitMQ["RabbitMQ<br/>消息队列"]
    end

    subgraph 数据层["🗄️ 数据层"]
        Postgres["PostgreSQL<br/>主数据库"]
        Redis["Redis<br/>缓存/Pub-Sub"]
    end

    Flutter -->|gRPC + WebSocket| Traefik
    React -->|gRPC-Web + WebSocket| Traefik
    
    Traefik -->|gRPC| Gateway
    Traefik -->|gRPC + WS| Chat
    
    Gateway -->|Auth 同步| Postgres
    Gateway -->|Command 异步| RabbitMQ
    RabbitMQ -->|消费消息| Workers
    
    Workers --> Postgres
    Workers --> Redis
    
    Chat --> Postgres
    Chat --> Redis
```

### 通信协议

> **重要**: 项目已完成通信协议统一化，全面采用 gRPC

| 协议 | 用途 | 说明 |
|------|------|------|
| **gRPC** | 所有 API 调用 | 强类型、高性能、跨语言 |
| **WebSocket** | 实时消息推送 | 仅用于 Chat 服务的实时消息 |

### 技术栈说明

| 层级 | 技术 | 说明 |
|------|------|------|
| 客户端 | Flutter 3.x | 跨平台移动端 + Web |
| 网关 | Traefik 3.x | 反向代理、负载均衡、gRPC/WebSocket 支持 |
| API 网关 | Go + gRPC | 统一 API 入口，消息发布 |
| 聊天服务 | Go + gRPC + WebSocket | 高性能实时聊天 |
| Worker 服务 | Go + RabbitMQ | 异步任务处理 |
| 消息队列 | RabbitMQ | 服务间异步通信 |
| 数据库 | PostgreSQL 16 | 主数据存储 |
| 缓存 | Redis 7 | 会话、缓存、Pub/Sub |
| 公共库 | service/pkg | 共享基础设施代码 |

---

## 前端架构 (Flutter)

### 目录结构概览

```
lib/
├── main.dart                    # 应用入口
├── core/                        # 核心基础设施
│   ├── grpc/                    # gRPC 客户端
│   │   ├── grpc_client.dart     # gRPC 连接管理器
│   │   ├── auth_grpc_client.dart # Auth gRPC 客户端
│   │   └── chat_grpc_client.dart # Chat gRPC 客户端
│   ├── di/                      # 依赖注入 (GetIt)
│   ├── router/                  # 路由配置 (GoRouter)
│   ├── theme/                   # 主题样式
│   ├── utils/                   # 工具函数
│   ├── errors/                  # 异常处理
│   ├── storage/                 # 本地存储
│   └── constants/               # 常量定义
├── features/                    # 功能模块 (按业务划分)
│   ├── auth/                    # 认证模块
│   ├── chat/                    # 聊天模块
│   ├── feeds/                   # 动态流模块
│   ├── post/                    # 帖子模块
│   ├── profile/                 # 个人资料
│   ├── search/                  # 搜索模块
│   ├── notifications/           # 通知模块
│   └── navigation/              # 导航模块
├── shared/                      # 共享组件
│   ├── widgets/                 # 通用 UI 组件
│   ├── models/                  # 共享数据模型
│   └── providers/               # 共享状态
└── generated/                   # 自动生成代码 (Proto)
    └── protos/
```

### gRPC 客户端架构

```
┌─────────────────────────────────────────────────────────┐
│  GrpcClientManager (grpc_client.dart)                   │
│  ├── 管理 gRPC Channel 连接                              │
│  ├── 提供认证 CallOptions                                │
│  └── 统一错误处理                                        │
└─────────────────────────────────────────────────────────┘
         │                              │
         ▼                              ▼
┌─────────────────────┐    ┌─────────────────────────────┐
│  AuthGrpcClient     │    │  ChatGrpcClient             │
│  ├── Gateway :50053 │    │  ├── Chat Service :50052    │
│  ├── Login          │    │  ├── GetConversations       │
│  ├── Register       │    │  ├── SendMessage            │
│  └── RefreshToken   │    │  ├── MarkAsRead             │
└─────────────────────┘    │  └── GetUnreadCounts        │
                           └─────────────────────────────┘
```

### Clean Architecture 分层详解

> 项目采用 Clean Architecture（整洁架构），将代码按职责分为三层。

#### 三层架构总览

```
用户点击按钮
    ↓
┌─────────────────────────────────────────────────────────┐
│  presentation/ (展示层) - 用户能看到的东西              │
│  ├── pages/     → 页面（LoginPage、HomePage）           │
│  ├── widgets/   → UI 组件（按钮、卡片、输入框）          │
│  └── providers/ → 状态管理（loading、error、数据）       │
└─────────────────────────────────────────────────────────┘
    ↓ 调用
┌─────────────────────────────────────────────────────────┐
│  domain/ (领域层) - 业务规则                            │
│  ├── entities/     → 业务对象（User、Post）             │
│  ├── repositories/ → 仓库接口（定义"能做什么"）          │
│  └── usecases/     → 用例（登录、发帖、点赞）            │
└─────────────────────────────────────────────────────────┘
    ↓ 调用
┌─────────────────────────────────────────────────────────┐
│  data/ (数据层) - 数据从哪来                            │
│  ├── models/       → 数据模型（Proto/JSON 转对象）      │
│  ├── datasources/  → 数据源（gRPC 请求、本地存储）       │
│  └── repositories/ → 仓库实现（具体怎么拿数据）          │
└─────────────────────────────────────────────────────────┘
    ↓
gRPC 服务 / 本地数据库
```

#### 各层职责说明

| 层级 | 目录 | 职责 | 示例 |
|------|------|------|------|
| **展示层** | `presentation/pages/` | 页面组件，组织 UI 布局 | `LoginPage`、`HomePage` |
| | `presentation/widgets/` | 可复用的 UI 组件 | 登录表单、帖子卡片 |
| | `presentation/providers/` | 状态管理 (Riverpod)，处理 loading/error/data | `AuthProvider`、`FeedProvider` |
| **领域层** | `domain/entities/` | 纯业务对象，不依赖任何框架 | `User`、`Post`、`Message` |
| | `domain/repositories/` | 仓库接口（抽象类），定义能做什么 | `AuthRepository.login()` |
| | `domain/usecases/` | 用例，封装单个业务操作 | `LoginUseCase`、`CreatePostUseCase` |
| **数据层** | `data/models/` | 数据模型，处理 Proto/JSON 序列化 | `UserModel.fromJson()` |
| | `data/datasources/` | 数据源，实际的 gRPC 调用或本地存储 | `ChatGrpcDataSourceImpl` |
| | `data/repositories/` | 仓库实现，协调数据源 | `AuthRepositoryImpl` |

---

## 后端架构

### Gateway + Worker 架构

> 架构采用 Gateway + RabbitMQ + Worker 模式，实现服务解耦和异步处理

```mermaid
flowchart TB
    subgraph Client["📱 客户端"]
        Flutter["Flutter App"]
        Web["Web Browser"]
    end

    subgraph Traefik["🌐 Traefik 网关 (:80)"]
        Router["路由分发"]
        CORS["CORS 中间件"]
    end

    subgraph Gateway["🚪 Go Gateway (:50053)"]
        GRPCServer["gRPC Server"]
        Publisher["RabbitMQ Publisher"]
    end

    subgraph MQ["📨 RabbitMQ"]
        AuthQueue["auth.* 队列"]
        UserQueue["user.* 队列"]
        PostQueue["post.* 队列"]
        FeedQueue["feed.* 队列"]
        NotifQueue["notification.* 队列"]
        SearchQueue["search.* 队列"]
    end

    subgraph Workers["⚙️ Worker 集群"]
        AuthWorker["Auth Worker<br/>认证/JWT"]
        UserWorker["User Worker<br/>用户管理"]
        PostWorker["Post Worker<br/>帖子处理"]
        FeedWorker["Feed Worker<br/>Feed 生成"]
        NotifWorker["Notification Worker<br/>通知推送"]
        SearchWorker["Search Worker<br/>搜索索引"]
    end

    subgraph DataStores["🗄️ 数据存储"]
        PG["PostgreSQL"]
        RD["Redis"]
    end

    %% 客户端到网关
    Flutter -->|gRPC| Traefik
    Web -->|gRPC-Web| Traefik

    %% Traefik 路由
    Router -->|/grpc/*| Gateway

    %% Gateway 发布消息
    GRPCServer --> Publisher
    Publisher --> AuthQueue
    Publisher --> UserQueue
    Publisher --> PostQueue
    Publisher --> FeedQueue
    Publisher --> NotifQueue
    Publisher --> SearchQueue

    %% Worker 消费消息
    AuthQueue --> AuthWorker
    UserQueue --> UserWorker
    PostQueue --> PostWorker
    FeedQueue --> FeedWorker
    NotifQueue --> NotifWorker
    SearchQueue --> SearchWorker

    %% Worker 访问数据
    AuthWorker --> PG
    AuthWorker --> RD
    UserWorker --> PG
    PostWorker --> PG
    FeedWorker --> PG
    FeedWorker --> RD
    NotifWorker --> PG
    SearchWorker --> PG

    style Gateway fill:#e1f5fe
    style MQ fill:#fff3e0
    style Workers fill:#e8f5e9
    style DataStores fill:#fce4ec
```

### 服务目录结构

```
service/
├── gateway/                    # API 网关服务
│   ├── cmd/server/
│   ├── internal/
│   │   ├── broker/            # RabbitMQ 发布者
│   │   └── server/            # gRPC 服务器
│   └── proto/                 # Proto 生成代码
│
├── pkg/                       # 共享公共库
│   ├── app/                   # 应用生命周期管理
│   ├── broker/                # RabbitMQ 消费引擎
│   ├── cache/                 # Redis 客户端封装
│   ├── config/                # 环境变量配置
│   ├── database/              # PostgreSQL 连接封装
│   ├── grpcclient/            # gRPC 客户端连接池
│   └── logger/                # Zap 日志封装
│
├── auth_worker/               # 认证 Worker
├── user_worker/               # 用户 Worker
├── post_worker/               # 帖子 Worker
├── feed_worker/               # Feed Worker
├── notification_worker/       # 通知 Worker
├── search_worker/             # 搜索 Worker
│
└── chat/                      # 聊天服务 (独立 gRPC + WebSocket)
    ├── cmd/server/
    └── internal/
        ├── handler/           # gRPC/WebSocket 处理器
        ├── middleware/        # 认证中间件
        ├── model/             # 数据模型
        ├── repository/        # 数据仓库
        ├── server/            # 服务器配置
        └── service/           # 业务逻辑
```

### 公共库 (pkg) 详解

| 模块 | 文件 | 功能 |
|------|------|------|
| **app** | `app.go` | 应用生命周期管理，统一启动/关闭 |
| **broker** | `worker.go` | RabbitMQ 消费引擎，自动重连，优雅停机 |
| **cache** | `redis.go`, `config.go` | Redis 客户端封装 |
| **config** | `config.go` | 环境变量读取工具 |
| **database** | `postgres.go` | PostgreSQL 连接池封装 |
| **grpcclient** | `pool.go`, `interceptors.go` | gRPC 客户端连接池 |
| **logger** | `logger.go` | Zap 日志封装，支持 trace_id |

### Worker 使用示例

```go
// service/post_worker/cmd/worker/main.go
package main

import (
    "context"
    "github.com/lesser/pkg/app"
    "github.com/lesser/pkg/broker"
    "github.com/lesser/post_worker/internal/service"
)

func main() {
    ctx := context.Background()

    // 1. 初始化应用（自动连接 DB、配置日志）
    cfg := app.ConfigFromEnv("post-worker")
    application, err := app.New(cfg)
    if err != nil {
        panic(err)
    }

    // 2. 创建业务服务
    postSvc := service.NewPostService(application.DB())

    // 3. 配置队列消费
    brokerConfigs := []broker.Config{
        {Queue: "post.create", Handler: postSvc.HandleCreate},
        {Queue: "post.delete", Handler: postSvc.HandleDelete},
    }

    // 4. 启动（阻塞直到收到停止信号）
    if err := application.Run(ctx, brokerConfigs...); err != nil {
        panic(err)
    }
}
```

### 请求处理流程

```mermaid
sequenceDiagram
    participant Client as 客户端
    participant Traefik as Traefik
    participant Gateway as Gateway
    participant MQ as RabbitMQ
    participant Worker as Worker
    participant DB as PostgreSQL

    Client->>Traefik: gRPC 请求
    Traefik->>Gateway: 路由转发
    Gateway->>MQ: 发布消息到队列
    Gateway-->>Client: 返回请求 ID (异步)
    
    MQ->>Worker: 消费消息
    Worker->>DB: 执行业务逻辑
    Worker->>MQ: 发布结果/事件
```

### 服务职责划分

| Worker | 队列前缀 | 职责 |
|--------|----------|------|
| auth_worker | `auth.*` | 用户注册、登录、JWT 生成/验证 |
| user_worker | `user.*` | 用户资料、关注/粉丝关系 |
| post_worker | `post.*` | 帖子 CRUD、Story 过期处理 |
| feed_worker | `feed.*` | Feed 生成、点赞/评论/转发 |
| notification_worker | `notification.*` | 通知创建、推送 |
| search_worker | `search.*` | 搜索索引更新 |

---

### Chat 服务架构

> Go Chat Service 负责高性能实时聊天功能，独立于 Worker 架构，直接提供 gRPC 服务

```mermaid
flowchart TB
    subgraph Entry["🚪 入口层"]
        GRPC["gRPC Server :50052"]
        WS["WebSocket Hub :8081<br/>实时消息分发"]
    end

    subgraph Handlers["🎯 处理层"]
        ChatGRPCHandler["ChatServicer<br/>聊天 gRPC 服务"]
        Hub["Hub<br/>连接管理"]
    end

    subgraph Services["⚙️ 服务层"]
        ChatService["ChatService<br/>聊天业务逻辑"]
        UserClient["UserClient<br/>用户信息 (Redis 缓存)"]
        UnreadCache["UnreadCacheService<br/>未读消息缓存"]
    end

    subgraph Repository["🗃️ 仓库层"]
        ConvRepo["ConversationRepo"]
        MsgRepo["MessageRepo"]
    end

    subgraph External["🔗 外部依赖"]
        PG["PostgreSQL<br/>lesser_chat_db"]
        Redis["Redis"]
    end

    GRPC --> ChatGRPCHandler
    WS --> Hub

    ChatGRPCHandler --> ChatService
    Hub --> ChatService

    ChatService --> UserClient
    ChatService --> UnreadCache
    ChatService --> ConvRepo
    ChatService --> MsgRepo

    ConvRepo --> PG
    MsgRepo --> PG
    UserClient --> Redis
    UnreadCache --> Redis

    style Entry fill:#e8f5e9
    style Services fill:#fff3e0
    style External fill:#fce4ec
```

### Chat gRPC API

| RPC 方法 | 说明 |
|----------|------|
| `GetConversations` | 获取会话列表 |
| `GetConversation` | 获取会话详情 |
| `CreateConversation` | 创建会话 |
| `GetMessages` | 获取消息列表 |
| `SendMessage` | 发送消息 |
| `StreamMessages` | 实时消息流 |
| `MarkAsRead` | 标记单条消息已读 |
| `MarkConversationAsRead` | 标记会话已读 |
| `GetUnreadCounts` | 批量获取未读数 |

### Gateway Auth API (同步)

| RPC 方法 | 说明 |
|----------|------|
| `Login` | 用户登录，同步返回 JWT |
| `Register` | 用户注册，同步返回 JWT |
| `RefreshToken` | 刷新 Token |

### WebSocket 消息格式

**客户端 → 服务端**
```json
{"action": "subscribe", "conversation_id": "uuid"}
{"action": "unsubscribe", "conversation_id": "uuid"}
{"action": "ping"}
```

**服务端 → 客户端**
```json
{"type": "message", "payload": {...}}
{"type": "conversation_update", "payload": {...}}
{"type": "messages_read", "payload": {...}}
{"type": "unread_update", "payload": {"count": 5}}
{"type": "pong"}
```

---

## 新增路由修改流程

> 添加新 API 路由时，需要修改以下文件

### 新增 gRPC API (通过 Gateway)

| 步骤 | 文件/目录 | 说明 |
|------|----------|------|
| 1 | `protos/<service>/<service>.proto` | 定义 gRPC 消息和服务 |
| 2 | `service/gateway/internal/server/` | Gateway 处理器 |
| 3 | `service/<xxx>_worker/internal/worker/` | Worker 消息处理 |
| 4 | `service/<xxx>_worker/internal/service/` | 业务逻辑实现 |
| 5 | `infra/gateway/dynamic/routes.yml` | Traefik 路由 (如需新前缀) |
| 6 | `client/mobile_flutter/lib/features/<module>/` | 客户端模块 |

### 新增 Chat gRPC API

| 步骤 | 文件/目录 | 说明 |
|------|----------|------|
| 1 | `protos/chat/chat.proto` | 定义 gRPC 消息和服务 |
| 2 | `service/chat/internal/handler/grpc/` | gRPC 处理器 |
| 3 | `service/chat/internal/service/` | 业务逻辑 |
| 4 | `service/chat/internal/repository/` | 数据仓库 |
| 5 | `client/mobile_flutter/lib/core/grpc/chat_grpc_client.dart` | Flutter gRPC 客户端 |

---

## 配置参数汇总

### 环境变量

| 变量名 | 默认值 | 说明 |
|--------|--------|------|
| `RABBITMQ_URL` | `amqp://guest:guest@localhost:5672/` | RabbitMQ 连接地址 |
| `DB_HOST` | `localhost` | PostgreSQL 主机 |
| `DB_PORT` | `5432` | PostgreSQL 端口 |
| `DB_USER` | `postgres` | 数据库用户 |
| `DB_PASSWORD` | `postgres` | 数据库密码 |
| `DB_NAME` | `lesser_db` | 数据库名称 |
| `REDIS_URL` | `redis://localhost:6379` | Redis 连接地址 |
| `JWT_SECRET` | - | JWT 签名密钥 |
| `ENV` | - | 设为 `production` 启用 JSON 日志 |

### 数据库配置

| 服务 | 数据库 | 连接池 |
|------|--------|--------|
| Workers | lesser_db | 由 pkg/database 管理 |
| Chat | lesser_chat_db | MaxOpenConns: 25, MaxIdleConns: 10 |

### 缓存配置

| 缓存项 | TTL | 键格式 |
|--------|-----|--------|
| 用户信息 | 5 分钟 | `user:info:{userID}` |
| 未读消息数 | 24 小时 | `unread:{userID}:{convID}` |

### 服务端口

| 服务 | HTTP | gRPC | 说明 |
|------|------|------|------|
| Traefik | 80 | - | 统一入口 |
| Gateway | - | 50053 | API 网关 |
| Chat | 8081 | 50052 | 聊天服务 |
| RabbitMQ | 15672 | 5672 | 消息队列 |
| PostgreSQL | - | 5432 | 数据库 |
| Redis | - | 6379 | 缓存 |

### Flutter 客户端配置

| 配置项 | 值 | 说明 |
|--------|-----|------|
| `grpcHost` | `localhost` | gRPC 服务主机 |
| `grpcPort` | `50053` | Gateway gRPC 端口 |
| `chatGrpcPort` | `50052` | Chat Service gRPC 端口 |
| `wsBaseUrl` | `ws://localhost:80` | WebSocket 基础 URL |

---

## Docker 服务列表

| 服务 | 容器名 | 说明 |
|------|--------|------|
| traefik | traefik | API 网关 |
| postgres | postgres | 数据库 |
| redis | redis | 缓存 |
| rabbitmq | rabbitmq | 消息队列 |
| gateway | gateway | Go Gateway |
| chat | chat | Chat 服务 |
| auth-worker | auth-worker | 认证 Worker |
| user-worker | user-worker | 用户 Worker |
| post-worker | post-worker | 帖子 Worker |
| feed-worker | feed-worker | Feed Worker |
| notification-worker | notification-worker | 通知 Worker |
| search-worker | search-worker | 搜索 Worker |
| dozzle | dozzle | 日志查看器 |
