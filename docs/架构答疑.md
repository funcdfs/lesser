# 架构答疑

> 基于 [架构梳理](./架构梳理.md) 文档的深入解读，结合实际代码（如 `docker-compose.yml`, `stream.go`, `content_client.go` 等）进行详细拆解。

---

## Q1: 如何从单容器扩展到集群？

架构图中每个小方框代表**一个服务的逻辑节点（单实例容器）**。在实际的生产环境中，扩展到一个具体的集群主要依靠以下手段和流程。微服务之间通过 `app-network` 内部网络通信，不暴漏在公网中。

### 扩展手段与机制

1. **容器编排层水平扩展 (Horizontal Scaling)**
   - **Docker Compose**：对于中小型部署，可以在 `docker-compose.prod.yml` 中直接使用 `deploy.replicas: N` 来启动多个同名容器实例。
   - **Kubernetes (K8s)**：通过配置 `Deployment` 的 `replicas`，或者结合 HPA (Horizontal Pod Autoscaler) 根据 CPU/内存/自定义指标自动弹性伸缩 Pod 数量。
2. **边缘网关服务发现与负载均衡 (Traefik)**
   - Traefik 挂载了 `/var/run/docker.sock`，能够实时监听容器的启动与销毁事件（自动服务发现）。
   - 当检测到某个服务（如 `User` 服务）存在多个实例时，Traefik 会自动将外部流量以**轮询 (Round-Robin)** 或其他策略分发到这些实例的 IP 上。
3. **无状态设计 (Stateless) 与有状态服务的解耦**
   - 绝大多数业务服务（Auth、User、Content 等）都是完全无状态的，可以任意增加节点。
   - 状态（如 Session、长连接映射、业务数据）被剥离并下沉到高可用的数据基座中（Redis 主从/哨兵集群、PostgreSQL 主从同步、RabbitMQ 镜像队列）。

### 集群部署拓扑图

```mermaid
graph TB
    subgraph Client["客户端层"]
        Flutter["Flutter App/Web"]
    end

    subgraph Edge["网关层 (负载均衡)"]
        Traefik["Traefik 网关集群<br/>(监听容器变化, 动态路由)"]
    end

    subgraph Stateless["无状态业务服务集群 (可任意伸缩)"]
        direction LR
        GW1["Gateway 实例 1"]
        GWn["Gateway 实例 n"]
        Auth1["Auth 实例 1"]
        Authn["Auth 实例 n"]
        Content1["Content 实例 1..."]
    end

    subgraph Stateful["有状态服务集群 (长连接与状态管理)"]
        Chat1["Chat 实例 1"]
        Chatn["Chat 实例 n"]
    end

    subgraph Storage["高可用数据底座"]
        PG[("PostgreSQL 主从集群")]
        Redis[("Redis 哨兵/集群模式")]
        MQ[("RabbitMQ 镜像队列集群")]
    end

    Flutter -->|流量入口| Traefik
    Traefik -->|路由分发| GW1 & GWn
    Traefik -->|gRPC-Web/Stream| Chat1 & Chatn
    
    GW1 & GWn --> Auth1 & Authn
    GW1 & GWn --> Content1
    
    Auth1 & Content1 --> PG
    Content1 --> MQ
    Chat1 & Chatn -. "Pub/Sub 协调状态" .-> Redis
    Chat1 & Chatn --> PG
```

---

## Q2: Content 与 Comment 之间的通信机制

这两个服务之间通过 **gRPC (同步 RPC)** 和 **RabbitMQ (异步 MQ 事件)** 协同工作。在理解两种通信模式之前，需要先弄清楚"Comment 调用 Content 的 gRPC"这件事是如何在 Docker 容器之间打通的。

---

### 深入理解：gRPC 跨容器调用的四层原理

#### 第一层：Docker 内网（`app-network`）提供 DNS 解析

所有服务都加入了同一个 Docker Bridge 网络 `app-network`。Docker 会自动为**每个服务名**注册一条内部 DNS 记录，使得同一网络内的容器可以通过服务名直接互相访问，公网完全不可见：

```yaml
# docker-compose.yml - 每个服务都声明加入同一网络
networks:
  - app-network
```

| 服务名（内部 DNS 域名） | Docker 分配内部 IP | 监听端口 |
|---|---|---|
| `content` | 172.18.0.x（自动分配）| 50054 |
| `comment` | 172.18.0.y（自动分配）| 50055 |
| `postgres` | 172.18.0.z（自动分配）| 5432 |

在任何容器内部执行 `ping content`，Docker 内置的 DNS 服务器都会把 `content` 解析为对方容器的真实内部 IP。`content:50054` 这个地址就是这样工作的。

#### 第二层：`comment` 启动时通过环境变量注入目标地址

```yaml
# docker-compose.yml - comment 服务配置（第 376 行）
comment:
  environment:
    - CONTENT_SERVICE_ADDR=content:50054  # ← 启动时注入 Content 的"电话号码"
  depends_on:
    content:
      condition: service_healthy           # ← 确保 Content 健康后才启动
```

`CONTENT_SERVICE_ADDR=content:50054` 这个环境变量在 **Comment 容器启动时**被读入，Comment 进程由此知道了 Content 服务的连接地址。

#### 第三层：`ContentServiceClient` 建立 TCP 连接池（长连接）

Comment 服务初始化时，读取这个地址，调用 `NewContentServiceClient`，向 `content:50054` 发起真实的 TCP 连接并维护连接池（`comment/internal/remote/content_client.go`）：

```go
// NewContentServiceClient 在服务启动时被调用一次，建立到 Content 的连接池
func NewContentServiceClient(addr string, log *log.Logger) (*ContentServiceClient, error) {
    pool := client.NewPool(log)

    cfg := client.DefaultConfig()
    cfg.Target = addr          // addr = "content:50054"（来自环境变量）
    pool.Register(contentServiceName, cfg)

    return &ContentServiceClient{pool: pool, ...}, nil
}
```

`pool` 在后台与 `content:50054` 建立若干条 **HTTP/2 TCP 长连接**并复用，避免每次调用都重新握手，这是 gRPC 性能高于 REST 的核心原因之一。

#### 第四层：每次 gRPC 调用 = 在 TCP 连接上发送一帧 Protobuf

当 `CreateComment` 逻辑调用 `CheckContentExists` 时：

```go
// 每次请求时的调用路径
func (c *ContentServiceClient) CheckContentExists(ctx context.Context, contentID string) (...) {
    conn, err := c.pool.GetConn(ctx, contentServiceName) // 1. 从连接池取出已有 TCP 连接

    client := contentpb.NewContentServiceClient(conn)    // 2. 把连接包装成 gRPC Stub
    resp, err := client.CheckContentExists(ctx, &contentpb.CheckContentExistsRequest{
        ContentId: contentID,                            // 3. 参数序列化为 Protobuf 二进制帧
    })                                                   // 4. 帧写入 TCP，等待响应帧

    return resp.Exists, resp.CommentsDisabled, nil       // 5. 反序列化响应并返回
}
```

底层发生的事：
1. 请求参数 → **Protobuf 序列化**成紧凑的二进制数据（相比 JSON 体积更小）
2. 数据包装成 **HTTP/2 帧**，写入已建立好的 TCP 连接（指向 `content:50054`）
3. Content 容器里的 gRPC Server 在 50054 端口收到这一帧数据
4. **反序列化** → 调用 Content 内部的 `CheckContentExists` Go 函数 → **序列化响应**写回同一 TCP 连接
5. Comment 收到响应帧，反序列化后，像调用本地函数一样直接返回结果

```
[Comment 容器]                   Docker 内网 app-network                [Content 容器]
      │                                                                       │
      │  pool.GetConn() → 取出已建立的 TCP 连接                               │
      │  Protobuf 序列化请求 ─────────────────────────────TCP帧──────────►   │
      │                                               gRPC Server 收到帧      │
      │                                               调用本地 Go 函数        │
      │  ◄──────────────────────────────────TCP帧──── 序列化响应写回          │
      │  反序列化，函数返回结果                                                │
```

> **一句话总结**：gRPC 跨 Docker 调用 = Docker 内网 DNS（服务名→IP）+ TCP 长连接池 + Protobuf 序列化帧。框架将这一切封装，让调用方感觉像在调用本地函数。只要两个容器在同一个 `app-network` 内，就能打通，与"是否在不同 Docker 容器"无关。

---

### 同步链路：gRPC（强一致性与业务前置校验）

当用户发生评论行为时，`Comment` 服务需要**同步知道**该内容是否存在、是否被锁定，以及通知 `Content` 更新评论计数：

- **调用流**: `CheckContentExists` 查状态 → 本地写评论持久化 → `UpdateCounter` 增加评论计数
- **强一致性保证**: 整个链路是同步阻塞的，任意一步失败都会回滚，用户会收到明确的错误响应

### 异步链路：RabbitMQ（最终一致性与旁路分发）

评论完成后，通知、搜索索引更新等**无需阻塞用户**的旁路逻辑通过 MQ 异步处理：

- **发送端（Publisher）**: `Comment` 服务在数据库事务提交后，向 RabbitMQ 的 `gateway.direct` 交换机异步发送 `comment.created` 事件
- **接收端（Consumer）**: `Notification` 服务监听 `notification.comment.created` 队列进行红点推送；`Search` 服务监听队列进行 ES 索引写入

### Content 与 Comment 完整交互时序图

```mermaid
sequenceDiagram
    participant App as Flutter Client
    participant GW as Gateway
    participant Comment as Comment Service
    participant Content as Content Service
    participant PG as PostgreSQL
    participant MQ as RabbitMQ Exchange
    participant Notify as Notification Service

    App->>GW: POST /comment (含 Auth Token)
    GW->>Comment: gRPC CreateComment(x-user-id)

    rect rgb(240, 248, 255)
        Note right of Comment: 1. 同步 gRPC 校验环节（CheckContentExists）
        Note right of Comment: Comment 通过连接池向 content:50054 发送 Protobuf 帧
        Comment->>Content: 内部 gRPC: CheckContentExists(contentId)
        Content-->>Comment: 返回 exists / commentsDisabled
    end

    Comment->>PG: INSERT 评论数据（持久化）

    rect rgb(240, 248, 255)
        Note right of Comment: 2. 同步 gRPC 更新聚合计数（UpdateCounter）
        Comment->>Content: 内部 gRPC: UpdateCounter(commentCount+1)
        Content-->>Comment: 返回 newCount
    end

    Comment-->>GW: 评论创建成功
    GW-->>App: 200 OK

    rect rgb(255, 245, 238)
        Note right of Comment: 3. 异步 MQ 旁路通知（不阻塞主流程）
        Comment-)MQ: Publish(event='comment.created')
        MQ-)Notify: Push to Queue(notification.comment.created)
        Notify->>Notify: 解析事件，为内容作者创建未读提醒
    end
```

---

## Q3: 两个用户登录 Flutter 之后的 Chat 通信流程

在聊天服务 (`Chat`) 中，核心组件是管理 WebSocket / gRPC 双向流的 `StreamManager` (`stream.go`)。

### 1. 通信承载与长连接落点
- **长连接技术选型**: Flutter 客户端通过 **gRPC-Web / gRPC Bidirectional Streaming** 与后端建立真正的长连接。
- **落点**: 长连接会打到达具体的某一个 `Chat` 容器实例上（比如 Chat-Instance-A）。这是由边缘网关 Traefik 的负载均衡决定的。连接建立后，在 `stream.go` 的 `StreamManager.clients` 字典中，会维持该 `user_id` 与对应 `grpc.ServerStream` 的生命周期映射。

### 2. 跨机器（实例）的消息路由
如果 User A 连在 `Chat-Instance-1`，User B 连在 `Chat-Instance-2`。
- A 通过长连接将发送消息 (`SendMessage`) 的请求发送给 `Chat-Instance-1`。
- `Chat-Instance-1` 会验证权限、将消息持久化至 PostgreSQL 的 `lesser_chat_db`。
- `Chat-Instance-1` 会通过 `Redis` 的 **Pub/Sub 机制**（代码见 `chat_service.go` 的 `s.cache.Publish(ctx, channel, msg)`），将该消息体广播到名为 `conversation:{conv_id}` 的频道中。
- 所有 `Chat` 的实例都在持续监听 Redis 的这些频道。
- `Chat-Instance-2` 从 Redis 收到了该消息，它检查自己本地的 `clients` 字典，发现 User B 就在自己这，于是直接向 User B 的推流通道 (`stream.Send`) 写入并下发该消息。

### 实时通信完整包流转图

```mermaid
sequenceDiagram
    participant UserA as User A Client
    participant Traefik as Traefik
    participant Chat1 as Chat Instance 1
    participant DB as lesser_chat_db (PG)
    participant Redis as Redis (Pub/Sub)
    participant Chat2 as Chat Instance 2
    participant UserB as User B Client

    Note over UserA, Chat1: A 登录并维持 gRPC Stream (TCP)
    Note over UserB, Chat2: B 登录并维持 gRPC Stream (TCP)

    UserA->>Chat1: SendMessage(B, "Hello")
    
    Chat1->>Chat1: 鉴权与业务逻辑校验
    Chat1->>DB: INSERT Message (持久化)
    
    par 发送确认与本地广播
        Chat1-->>UserA: MessageSent ACK (上屏)
        Note over Chat1: 检查本地由于没有连着 B
    and Redis 集群广播
        Chat1->>Redis: PUBLISH conversation:{id} payload
    end
    
    Redis-->>Chat2: 触发 SUBSCRIBE 事件回调
    
    Chat2->>Chat2: 检索 StreamManager.clients 找到 User B
    Chat2->>UserB: stream.Send(NewMessage) 推送下发
```

---

## Q4: Traefik 网关与具体业务 Gateway 的区别

系统采用了 **“边缘代理 + 业务网关” （Dual-Gateway）** 的双层架构设计。

### 1. Traefik 的定位：边缘代理 (Edge Proxy)
Traefik 是一个纯基础设施层的反向代理，它是进出集群的唯一大门（主要暴露端口 80, 50050）。
**作用**：
- **TLS 终结**：负责处理 HTTPS 证书卸载（在生产环境）。
- **四层/七层路由**：将 `/grpc.ChatService/` 路由到 Chat 容器，将 `/api/v1/` 路由到业务 Gateway。
- **服务发现**：自动感知 Docker 或 K8s 中启动或死掉的容器，并动态调整负载均规则，**不需要修改或重载配置文件**。
- **协议转换**：具备翻译机制，可以将前端的 gRPC-Web HTTP/1.1 请求自动转换为后端的原生 HTTP/2 gRPC 调用。

### 2. Gateway 的定位：业务网关 (Business API Gateway)
位于 `service/gateway/` 下，是用 Go 编写的一个具体微服务。它是所有普通业务请求的第一站总控台。
**作用**：
- **全局身份鉴权 (Auth)**：客户端发来的 Http metadata 中只带 `Authorization: Bearer <jwt-token>`。Gateway 负责拦截这些请求，去调用 `Auth` 服务解密 Token，抽取出内部系统的全局唯一标识 `user_id`，并重新注入到 gRPC 的 Metadata `x-user-id` 中。
- **剥离信任边界**：下游的服务（如 User, Content, Comment）**不再自己去解析和校验 JWT 的合法性**。只要请求来自于 Gateway（携带有特定的内部头），下游就无条件信任 `x-user-id`。这让下游的业务代码变得极度精简纯粹。
- **限流熔断**：可以针对具体的 Endpoint 做精细化的业务级别限流。

### 为什么要分开？为什么要单独写一个 Gateway？

1. **职责分离法则**：
   Traefik 不懂“业务逻辑”（它不知道啥是 JWT 的具体声明、啥是内部加密秘钥）；而业务 Gateway 不应该处理底层的 Socket 均衡策略、SSL证书和连接池维护。让专业的组件干专业的事。
2. **长连接的性能避坑优化**：
   你可以注意到架构图里，`Chat` 和 `Channel` 服务的长连接流量是 **直接由 Traefik 路由给它们** 的，绕开了中间的 Gateway。这是有意为之的。
	  
	**原因所在**：Gateway 中存在诸多的业务拦截器 (Interceptors) ，例如 JWT 校验。让成千上万个双向常连接 (gRPC Bidirectional Stream) 长期穿透一个“鉴权”用的 Gateway，不仅资源浪费，还会放大 Gateway 崩溃对全局实时系统的雪崩效应。**因此**，`Chat` 内部自己做了对应的安检体系并独立承载长连接（代码可见 `getUserIDFromStreamContext`），以减轻业务网关的压力。这使得系统拥有极佳的可扩展性与解耦度。

```mermaid
graph TD
    subgraph Client["客户端域"]
        Flutter["Flutter Client"]
    end

    subgraph Traefik["Traefik (边缘反向代理)"]
        TraefikRoute[("四层/七层路由匹配规则")]
    end

    subgraph Service["微服务域 (内网安全区)"]
        GW["定制开发 Gateway (50051)"]
        Chat["Chat (50060 - 独立认证保护)"]
        Auth["Auth (50052)"]
        User["User (50053)"]
    end

    Flutter -->|HTTP/TCP 流量| Traefik
    
    Traefik --"流量判定 HTTP /api/xxx"--> GW
    Traefik --"流量判定 gRPC 流 /ChatService/..."--> Chat
    
    GW --"发往 Auth 校验 token"--> Auth
    Auth --"返回 valid"--> GW
    
    GW --"注入 x-user-id 到 Metadata"--> User
    
    %% 这里体现出安全边界
    style GW fill:#f9f,stroke:#333,stroke-width:2px;
    style Chat fill:#bbf,stroke:#333,stroke-width:2px;
```
